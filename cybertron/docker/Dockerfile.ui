FROM python:3.11-slim

WORKDIR /app

# Install system deps
RUN apt-get update && apt-get install -y \
    gcc libffi-dev \
    && rm -rf /var/lib/apt/lists/*

# Install Python deps
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy UI files
COPY *.py ./
COPY *.html ./
COPY *.json ./

# Expose web UI port
EXPOSE 8080

CMD ["python3", "cybertron_ui.py", "web"]
